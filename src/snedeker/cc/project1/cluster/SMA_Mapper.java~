package snedeker.cc.project1.cluster;

public class SMA_Mapper extends Mapper<LongWritable, Text, Text, Time_series> {
	//call Writable objects by Hadoop
	private final Text RKey = new Text();
	private final Time_Series RValue = new Time_Series();
	
	public void map(LongWritable key, Text value, Context context) throws IOException, InterruptedException {
		
	}
}
